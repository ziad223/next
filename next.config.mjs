/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
        domains : ['cdn.bynogame.com' , 'dailycard-shop.com' , 'dailycard.future-developers.cloud' , 'c0.klipartz.com']
    }
};

export default nextConfig;
